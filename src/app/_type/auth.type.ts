export type Auth = {
    idLingua: number,
    tk: string | null,
    nome: string | null,
    idRuolo: number | null,
    idStato: number | null,
    abilita: number[] | null
    idUtente: number | null
}

//tipo che estrapola i dati per autenticazione

